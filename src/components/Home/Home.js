import React from "react";
import styled from "styled-components";

const HomeWrapper = styled.div``;

const Home = () => {
  return (
    <HomeWrapper>
      <div></div>
    </HomeWrapper>
  );
};

export default Home;
